import type { ManualOverrides, MatchRecord, SourceMetadata } from './types.ts';

interface ReviewSummary {
  visibleExerciseCount: number;
  tiers: Record<string, number>;
  media: Record<string, number>;
}

export const REVIEW_STORAGE_KEY = 'musclemap-fitness:free-exercise-db-review:v0.1';

export function createReviewPage(matches: MatchRecord[], summary: ReviewSummary, source: SourceMetadata, initialOverrides?: ManualOverrides) {
  const embedded = JSON.stringify({ matches, summary, source, initialOverrides: initialOverrides ?? emptyOverrides() }).replace(/</g, '\\u003c');
  return `<!doctype html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="color-scheme" content="light">
  <title>Free Exercise DB 交互审核</title>
  <style>
    :root{font-family:Inter,ui-sans-serif,system-ui,-apple-system,"Segoe UI",sans-serif;color:#162033;background:#eef1f6;--ink:#162033;--muted:#596579;--line:#ccd3df;--surface:#fff;--soft:#f6f8fb;--accent:#3157c8;--accept:#19704f;--force:#ad5b12;--reuse:#1769aa;--danger:#a6343f;--focus:0 0 0 3px rgba(49,87,200,.2)}*{box-sizing:border-box}body{margin:0;min-width:1024px;background:#eef1f6}button,input,select,textarea{font:inherit}button{cursor:pointer}.topbar{background:#172033;color:#fff;padding:18px max(24px,calc((100vw - 1680px)/2));display:flex;align-items:center;justify-content:space-between;gap:24px}.topbar h1{margin:0;font-size:1.35rem;letter-spacing:-.02em}.topbar p{margin:4px 0 0;color:#c8d0df;font-size:.78rem}.top-actions,.button-row,.quick-row,.nav-row{display:flex;flex-wrap:wrap;gap:8px}.page{max-width:1680px;margin:auto;padding:18px 24px 32px}.progress{display:grid;grid-template-columns:repeat(8,minmax(105px,1fr));border:1px solid var(--line);border-radius:12px;overflow:hidden;background:var(--line);gap:1px}.metric{background:#fff;padding:11px 13px}.metric strong{display:block;font-size:1.2rem;font-variant-numeric:tabular-nums}.metric span{font-size:.72rem;color:var(--muted)}.toolbar{position:sticky;top:0;z-index:5;margin:14px 0;padding:12px;background:rgba(255,255,255,.97);border:1px solid var(--line);border-radius:12px}.filters{display:grid;grid-template-columns:minmax(210px,1.6fr) repeat(4,minmax(130px,1fr));gap:9px}.field{display:grid;gap:4px;color:var(--muted);font-size:.72rem;font-weight:700}.field input,.field select,.field textarea{width:100%;min-height:38px;border:1px solid #b9c2d1;border-radius:8px;background:#fff;color:var(--ink);padding:7px 9px;outline:none}.field textarea{min-height:72px;resize:vertical}.field input:focus,.field select:focus,.field textarea:focus{border-color:var(--accent);box-shadow:var(--focus)}.tier-fieldset{grid-column:1/-1;border:0;padding:0;margin:0;display:flex;align-items:center;gap:8px;flex-wrap:wrap}.tier-fieldset legend{float:left;margin-right:4px;color:var(--muted);font-size:.72rem;font-weight:750}.check{display:inline-flex;align-items:center;gap:5px;min-height:32px;padding:4px 8px;border:1px solid var(--line);border-radius:999px;background:#fff;color:#334158;font-size:.75rem}.check input{accent-color:var(--accent)}.quick-row{margin-top:9px}.btn{min-height:36px;border:1px solid #aeb8c8;border-radius:8px;background:#fff;color:#25334a;padding:7px 11px;font-size:.78rem;font-weight:720}.btn:hover{background:#f4f6fa}.btn:focus-visible{outline:none;box-shadow:var(--focus)}.btn.primary{border-color:var(--accent);background:var(--accent);color:#fff}.btn.accept{border-color:var(--accept);background:var(--accept);color:#fff}.btn.force{border-color:var(--force);background:#fff7ed;color:#7d3c05}.btn.reuse{border-color:var(--reuse);background:#eef7ff;color:#0d548d}.btn.danger{border-color:#d7a9ae;color:#8e2832}.btn:disabled{cursor:not-allowed;opacity:.45}.workspace{background:#fff;border:1px solid var(--line);border-radius:12px;overflow:hidden}.navigator{display:flex;justify-content:space-between;align-items:center;gap:12px;padding:10px 14px;border-bottom:1px solid var(--line);background:#f8f9fc}.position{font-size:.78rem;color:var(--muted);font-variant-numeric:tabular-nums}.jump{display:flex;align-items:center;gap:6px}.jump input{width:230px;min-height:36px;border:1px solid #b9c2d1;border-radius:8px;padding:7px 9px}.record-head{display:grid;grid-template-columns:1.4fr 1fr .8fr;gap:20px;padding:18px}.record-head h2{margin:0 0 4px;font-size:1.25rem}.record-head p{margin:4px 0;color:var(--muted);font-size:.8rem;line-height:1.45}.facts{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:6px}.fact{padding:7px 9px;background:var(--soft);border-radius:8px;font-size:.74rem}.fact b{display:block;color:var(--muted);font-size:.66rem;margin-bottom:2px}.badge{display:inline-flex;align-items:center;border-radius:999px;padding:5px 9px;color:#fff;font-size:.7rem;font-weight:780}.badge.exact{background:#25745d}.badge.high-confidence{background:#557f32}.badge.manual-review{background:#a46316}.badge.unmatched{background:#963d45}.badge.already-covered{background:#315875}.decision{margin-top:8px;font-size:.78rem;font-weight:750}.decision.accepted{color:var(--accept)}.decision.forced{color:var(--force)}.decision.reuse{color:var(--reuse)}.decision.rejected-all{color:var(--danger)}.decision.skipped{color:#6b5a1d}.candidate-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:1px;background:var(--line);border-top:1px solid var(--line);border-bottom:1px solid var(--line)}.candidate{appearance:none;text-align:left;min-width:0;border:0;background:#fbfcfe;color:var(--ink);padding:13px;position:relative}.candidate:hover{background:#f4f7fb}.candidate.selected{z-index:1;outline:3px solid var(--accent);outline-offset:-3px;background:#f4f7ff}.candidate.accepted{z-index:2;outline:3px solid var(--accept);outline-offset:-3px;background:#eef9f4}.candidate.forced{z-index:2;outline:3px solid var(--force);outline-offset:-3px;background:#fff7ed}.candidate.reuse{z-index:2;outline:3px solid var(--reuse);outline-offset:-3px;background:#eef7ff}.candidate.rejected{opacity:.46;background:#f5eeee}.candidate.rejected:after{content:"已拒绝";position:absolute;top:10px;right:10px;border-radius:999px;background:var(--danger);color:#fff;padding:4px 7px;font-size:.65rem;font-weight:800}.candidate h3{margin:0 58px 3px 0;font-size:.88rem}.candidate .meta{color:var(--muted);font-size:.72rem;line-height:1.4}.candidate-images{display:grid;grid-template-columns:1fr 1fr;gap:6px;margin:9px 0}.image-slot{min-height:245px;display:grid;place-items:center;background:#e9edf3;border-radius:8px;overflow:hidden;color:var(--muted);font-size:.72rem}.image-slot img{width:100%;aspect-ratio:4/5;object-fit:contain;display:block}.muscles{font-size:.7rem;line-height:1.45;color:#3d4b60}.scores{display:grid;grid-template-columns:repeat(2,1fr);gap:4px;margin-top:8px}.score{display:flex;justify-content:space-between;gap:8px;padding:4px 6px;background:#eef1f6;border-radius:6px;color:var(--muted);font-size:.68rem}.score b{color:#25334a;font-variant-numeric:tabular-nums}.signals{margin-top:8px;font-size:.7rem;line-height:1.45}.signals p{margin:5px 0}.signals .match{color:#176445}.signals .difference{color:#755211}.signals .conflict{padding:6px;border-radius:6px;background:#fff0f1;color:#8b2933}.review-panel{display:grid;grid-template-columns:1.35fr .9fr;gap:16px;padding:16px}.actions h3,.notes h3{font-size:.82rem;margin:0 0 8px}.action-help{margin:8px 0 0;color:var(--muted);font-size:.72rem}.notes .field{font-size:.7rem}.reuse-form{display:none;margin-top:12px;padding:12px;border:1px solid #9dc3e1;border-radius:10px;background:#f2f8fd}.reuse-form.open{display:grid;grid-template-columns:1fr 1fr;gap:9px}.reuse-form .wide{grid-column:1/-1}.empty{padding:54px 24px;text-align:center;color:var(--muted)}.shortcuts{margin-top:14px;border:1px solid var(--line);border-radius:10px;background:#fff;padding:10px 13px;color:var(--muted);font-size:.75rem}.shortcuts summary{cursor:pointer;color:#2c3c55;font-weight:750}.shortcuts p{line-height:1.65;margin:8px 0 0}.source-note{margin:14px 0 0;color:var(--muted);font-size:.7rem;line-height:1.55;word-break:break-word}.toast{position:fixed;right:22px;bottom:22px;z-index:20;max-width:420px;padding:10px 13px;border-radius:9px;background:#172033;color:#fff;font-size:.78rem;opacity:0;transform:translateY(8px);pointer-events:none;transition:opacity .18s ease,transform .18s ease}.toast.show{opacity:1;transform:translateY(0)}dialog{border:0;border-radius:12px;padding:0;max-width:480px;color:var(--ink)}dialog::backdrop{background:rgba(18,27,43,.55)}.dialog-body{padding:18px}.dialog-body h2{margin:0 0 8px;font-size:1.1rem}.dialog-body p{color:var(--muted);font-size:.8rem;line-height:1.55}@media(max-width:1180px){body{min-width:760px}.progress{grid-template-columns:repeat(4,1fr)}.filters{grid-template-columns:repeat(3,1fr)}.record-head{grid-template-columns:1fr}.candidate-grid{grid-template-columns:1fr}.image-slot{min-height:320px}.review-panel{grid-template-columns:1fr}}@media(prefers-reduced-motion:reduce){*{scroll-behavior:auto!important;transition:none!important}}
  </style>
</head>
<body>
  <header class="topbar">
    <div><h1>Free Exercise DB 交互审核</h1><p>决定自动保存在本机。导出后可覆盖脚本目录中的 manual-overrides.json。</p></div>
    <div class="top-actions"><button class="btn" id="import-button">导入已有 manual-overrides.json</button><input id="import-file" type="file" accept="application/json,.json" hidden><button class="btn" id="export-summary">导出 review-summary.json</button><button class="btn primary" id="export-overrides">导出 manual-overrides.json</button><button class="btn danger" id="clear-progress">清空当前审核进度</button></div>
  </header>
  <main class="page">
    <section class="progress" id="progress" aria-label="审核进度"></section>
    <section class="toolbar" aria-label="筛选条件">
      <div class="filters">
        <label class="field">搜索动作<input id="query" type="search" placeholder="exerciseId / 中文名 / 英文名"></label>
        <label class="field">审核状态<select id="review-status"><option value="">全部</option><option value="unreviewed">未审核</option><option value="accepted">已接受</option><option value="forced">已强制</option><option value="rejected-all">已拒绝全部候选</option><option value="reuse">已标记共图</option><option value="skipped">暂时跳过</option></select></label>
        <label class="field">本地媒体<select id="media"><option value="">全部</option></select></label>
        <label class="field">器械<select id="equipment"><option value="">全部</option></select></label>
        <label class="field">主练肌群<select id="muscle"><option value="">全部</option></select></label>
        <fieldset class="tier-fieldset"><legend>匹配等级</legend><label class="check"><input type="checkbox" name="tier" value="exact" checked>exact</label><label class="check"><input type="checkbox" name="tier" value="high-confidence" checked>high-confidence</label><label class="check"><input type="checkbox" name="tier" value="manual-review">manual-review</label><label class="check"><input type="checkbox" name="tier" value="unmatched">unmatched</label><label class="check"><input type="checkbox" name="tier" value="already-covered">already-covered</label></fieldset>
      </div>
      <div class="quick-row"><button class="btn" data-quick="strong">只看 exact + high-confidence</button><button class="btn" data-quick="manual">只看 manual-review</button><button class="btn" data-quick="unreviewed">只看未审核</button><button class="btn" data-quick="conflicts">只看存在硬冲突</button><button class="btn" data-quick="clear">清空筛选</button></div>
    </section>
    <section class="workspace">
      <div class="navigator"><div class="nav-row"><button class="btn" id="previous">← 上一条</button><button class="btn" id="next">下一条 →</button><button class="btn" id="next-unreviewed">下一条未审核</button><button class="btn" id="first-unreviewed">返回第一条未审核</button></div><div class="position" id="position"></div><div class="jump"><input id="jump-id" placeholder="跳转到 exerciseId"><button class="btn" id="jump-button">跳转</button></div></div>
      <div id="record"></div>
    </section>
    <details class="shortcuts"><summary>键盘快捷键</summary><p>A 接受 · R 拒绝 · F 强制 · G 共图 · S 稍后处理 · ←/K 上一条 · →/J 下一条 · 1/2/3 选择候选 · E 导出。输入框、选择框或备注区获得焦点时快捷键暂停。</p></details>
    <p class="source-note">localStorage key：${REVIEW_STORAGE_KEY}<br>数据源：${escapeHtml(source.sourceUrl)}<br>commit：${escapeHtml(source.commit)} · SHA-256：${escapeHtml(source.sha256)} · ${source.recordCount} 条 · Unlicense · 下载时间 ${escapeHtml(source.downloadedAt)}</p>
  </main>
  <dialog id="import-dialog"><div class="dialog-body"><h2>导入审核决定</h2><p id="import-message">请选择如何应用导入文件。合并会保留当前状态，并用导入文件中的同名决定覆盖；完全覆盖会清空当前状态。</p><div class="button-row"><button class="btn primary" id="import-merge">合并</button><button class="btn danger" id="import-replace">完全覆盖</button><button class="btn" id="import-cancel">取消</button></div></div></dialog>
  <div class="toast" id="toast" role="status" aria-live="polite"></div>
  <script id="report-data" type="application/json">${embedded}</script>
  <script>
    const STORAGE_KEY='${REVIEW_STORAGE_KEY}';
    const payload=JSON.parse(document.getElementById('report-data').textContent);
    const tierOrder={exact:0,'high-confidence':1,'manual-review':2,unmatched:3,'already-covered':4};
    const tierLabels={exact:'exact','high-confidence':'high-confidence','manual-review':'manual-review',unmatched:'unmatched','already-covered':'already-covered'};
    const statusLabels={unreviewed:'未审核',accepted:'已接受',forced:'已强制','rejected-all':'已拒绝全部候选',reuse:'已标记共图',skipped:'暂时跳过'};
    const matches=[...payload.matches].sort((a,b)=>(tierOrder[a.tier]-tierOrder[b.tier])||(b.confidence-a.confidence)||a.exercise.exerciseId.localeCompare(b.exercise.exerciseId));
    const exerciseIds=new Set(matches.map(item=>item.exercise.exerciseId));
    const esc=(value)=>String(value??'').replace(/[&<>"']/g,(character)=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[character]));
    const unique=(values)=>[...new Set(values.filter(Boolean))].sort((a,b)=>String(a).localeCompare(String(b),'zh-CN'));
    let state=loadState();
    let currentId=null;
    let selectedByExercise={};
    let conflictsOnly=false;
    let reuseOpenFor=null;
    let pendingImport=null;

    function blankState(){return {version:1,updatedAt:null,accepted:{},rejected:{},forced:{},reuse:{},notes:{},skipped:[]}}
    function sanitizeState(raw){const clean=blankState();if(!raw||typeof raw!=='object')return clean;clean.updatedAt=typeof raw.updatedAt==='string'?raw.updatedAt:null;for(const field of ['accepted','forced','notes']){if(raw[field]&&typeof raw[field]==='object'){for(const [id,value] of Object.entries(raw[field]))if(exerciseIds.has(id)&&typeof value==='string'&&value)clean[field][id]=value}}if(raw.rejected&&typeof raw.rejected==='object'){for(const [id,values] of Object.entries(raw.rejected)){if(exerciseIds.has(id)&&Array.isArray(values)){const list=unique(values.filter(value=>typeof value==='string'));if(list.length)clean.rejected[id]=list}}}if(raw.reuse&&typeof raw.reuse==='object'){for(const [id,value] of Object.entries(raw.reuse)){if(exerciseIds.has(id)&&value&&typeof value==='object'&&exerciseIds.has(value.baseExerciseId)&&typeof value.sourceId==='string'&&value.sourceId){clean.reuse[id]={baseExerciseId:value.baseExerciseId,sourceId:value.sourceId,reason:typeof value.reason==='string'?value.reason:'',differences:typeof value.differences==='string'?value.differences:''}}}}clean.skipped=unique(Array.isArray(raw.skipped)?raw.skipped.filter(id=>exerciseIds.has(id)):[]);resolveFinalConflicts(clean);return clean}
    function resolveFinalConflicts(target){for(const id of Object.keys(target.forced)){delete target.accepted[id];delete target.reuse[id]}for(const id of Object.keys(target.accepted))delete target.reuse[id]}
    function loadState(){try{const saved=localStorage.getItem(STORAGE_KEY);return sanitizeState(saved?JSON.parse(saved):payload.initialOverrides)}catch(error){showToast('无法读取本地进度，已使用报告内状态');return sanitizeState(payload.initialOverrides)}}
    function persist(){state.updatedAt=new Date().toISOString();try{localStorage.setItem(STORAGE_KEY,JSON.stringify(state))}catch(error){showToast('localStorage 写入失败，请尽快导出决定')}}
    function setFinal(id,type,value){delete state.accepted[id];delete state.forced[id];delete state.reuse[id];state[type][id]=value;state.skipped=state.skipped.filter(item=>item!==id)}
    function candidatesFor(item){const map=new Map();for(const candidate of [...(item.topCandidates||[]),...(item.rejectedCandidates||[])])map.set(candidate.sourceId,candidate);return [...map.values()].sort((a,b)=>b.score.finalConfidence-a.score.finalConfidence||a.sourceId.localeCompare(b.sourceId)).slice(0,3)}
    function isRejected(id,sourceId){return (state.rejected[id]||[]).includes(sourceId)}
    function statusOf(item){const id=item.exercise.exerciseId;if(state.accepted[id])return 'accepted';if(state.forced[id])return 'forced';if(state.reuse[id])return 'reuse';const candidates=candidatesFor(item);if(candidates.length&&candidates.every(candidate=>isRejected(id,candidate.sourceId)))return 'rejected-all';if(state.skipped.includes(id))return 'skipped';return 'unreviewed'}
    function isComplete(item){return ['accepted','forced','reuse','rejected-all'].includes(statusOf(item))}
    function selectedCandidate(item){const candidates=candidatesFor(item);const requested=selectedByExercise[item.exercise.exerciseId];const selected=candidates.find(candidate=>candidate.sourceId===requested&&!isRejected(item.exercise.exerciseId,candidate.sourceId))||candidates.find(candidate=>candidate.sourceId===item.bestCandidate?.sourceId&&!isRejected(item.exercise.exerciseId,candidate.sourceId))||candidates.find(candidate=>!isRejected(item.exercise.exerciseId,candidate.sourceId))||candidates[0]||null;if(selected)selectedByExercise[item.exercise.exerciseId]=selected.sourceId;return selected}
    function selectedTiers(){return new Set([...document.querySelectorAll('input[name="tier"]:checked')].map(input=>input.value))}
    function filteredMatches(){const query=document.getElementById('query').value.trim().toLowerCase();const status=document.getElementById('review-status').value;const media=document.getElementById('media').value;const equipment=document.getElementById('equipment').value;const muscle=document.getElementById('muscle').value;const tiers=selectedTiers();return matches.filter(item=>{const e=item.exercise;return tiers.has(item.tier)&&(!query||[e.exerciseId,e.name,e.nameEn].join(' ').toLowerCase().includes(query))&&(!status||statusOf(item)===status)&&(!media||e.mediaStatus===media)&&(!equipment||e.equipment.includes(equipment))&&(!muscle||e.primaryMuscles.includes(muscle))&&(!conflictsOnly||candidatesFor(item).some(candidate=>candidate.score.conflicts.length))})}
    function currentItem(){return matches.find(item=>item.exercise.exerciseId===currentId)||null}
    function ensureCurrent(filtered){if(filtered.some(item=>item.exercise.exerciseId===currentId))return;currentId=(filtered.find(item=>!isComplete(item))||filtered[0]||{}).exercise?.exerciseId||null}
    function render(){const filtered=filteredMatches();ensureCurrent(filtered);renderProgress();renderPosition(filtered);renderRecord(currentItem());}
    function renderProgress(){const strong=matches.filter(item=>item.tier==='exact'||item.tier==='high-confidence');const reviewed=strong.filter(isComplete).length;const values=[['已审核',reviewed+' / '+strong.length],['已接受',Object.keys(state.accepted).length],['已强制',Object.keys(state.forced).length],['标记共图',Object.keys(state.reuse).length],['拒绝全部',matches.filter(item=>statusOf(item)==='rejected-all').length],['暂时跳过',state.skipped.length],['剩余',Math.max(0,strong.length-reviewed)],['预计可接入',Object.keys(state.accepted).length+Object.keys(state.forced).length+Object.keys(state.reuse).length]];document.getElementById('progress').innerHTML=values.map(([label,value])=>'<div class="metric"><strong>'+esc(value)+'</strong><span>'+esc(label)+'</span></div>').join('')}
    function renderPosition(filtered){const index=filtered.findIndex(item=>item.exercise.exerciseId===currentId);document.getElementById('position').textContent=filtered.length?(index+1)+' / '+filtered.length:'0 / 0'}
    function renderRecord(item){const root=document.getElementById('record');if(!item){root.innerHTML='<div class="empty">当前筛选条件下没有动作</div>';return}const e=item.exercise;const candidates=candidatesFor(item);const selected=selectedCandidate(item);const status=statusOf(item);root.innerHTML='<div class="record-head"><div><h2>'+esc(e.name)+'</h2><p>'+esc(e.exerciseId)+' · '+esc(e.nameEn)+'</p><p>器械：'+esc(e.equipment.join(' / ')||'未标注')+'</p><p>主练：'+esc(e.primaryMuscles.join(' / ')||'未标注')+'<br>次要：'+esc(e.secondaryMuscles.join(' / ')||'未标注')+'</p></div><div class="facts"><div class="fact"><b>动作属性</b>'+esc([e.category,e.force,e.mechanic,e.laterality].filter(Boolean).join(' / ')||'未标注')+'</div><div class="fact"><b>本地媒体</b>'+esc(e.mediaStatus)+'</div><div class="fact"><b>匹配等级</b><span class="badge '+esc(item.tier)+'">'+esc(tierLabels[item.tier])+'</span></div><div class="fact"><b>总置信度</b>'+item.confidence.toFixed(3)+'</div></div><div><p>'+esc(item.tierReason)+'</p><p><b>推荐：</b>'+esc(item.recommendedAction)+'</p><div class="decision '+status+'">'+esc(statusLabels[status])+'</div></div></div>'+(candidates.length?'<div class="candidate-grid">'+candidates.map((candidate,index)=>candidateHtml(item,candidate,index,selected)).join('')+'</div>':'<div class="empty">暂无候选</div>')+'<div class="review-panel"><div class="actions"><h3>审核当前候选</h3><div class="button-row"><button class="btn accept" id="accept" '+disabled(!selected)+'>接受此匹配</button><button class="btn danger" id="reject" '+disabled(!selected)+'>拒绝此候选</button><button class="btn force" id="force" '+disabled(!selected)+'>强制采用此候选</button><button class="btn reuse" id="reuse-toggle" '+disabled(!selected)+'>标记为共图</button><button class="btn" id="skip">稍后处理</button></div><p class="action-help">当前候选：'+esc(selected?.sourceId||'无')+'。完成最终决定后将自动前往下一条未审核动作。</p>'+reuseForm(item,selected)+'</div><div class="notes"><h3>人工备注</h3><label class="field">按 exerciseId 自动保存<textarea id="note" placeholder="记录图片阶段、把手、握距或其他复核事项">'+esc(state.notes[e.exerciseId]||'')+'</textarea></label></div></div>';bindRecordEvents(item,candidates)}
    function finalCandidateClass(item,candidate){const id=item.exercise.exerciseId;if(state.accepted[id]===candidate.sourceId)return 'accepted';if(state.forced[id]===candidate.sourceId)return 'forced';if(state.reuse[id]?.sourceId===candidate.sourceId)return 'reuse';return ''}
    function candidateHtml(item,candidate,index,selected){const rejected=isRejected(item.exercise.exerciseId,candidate.sourceId);const finalClass=finalCandidateClass(item,candidate);const score=candidate.score;return '<button type="button" class="candidate '+(selected?.sourceId===candidate.sourceId?'selected ':'')+(finalClass?finalClass+' ':'')+(rejected?'rejected':'')+'" data-source-id="'+esc(candidate.sourceId)+'" aria-pressed="'+(selected?.sourceId===candidate.sourceId)+'"><h3>#'+(index+1)+' '+esc(candidate.sourceName)+'</h3><div class="meta">'+esc(candidate.sourceId)+' · '+esc(candidate.sourceEquipment||'未知器械')+'</div><div class="candidate-images">'+imageHtml(candidate.startImageUrl,candidate.sourceName+' start')+imageHtml(candidate.peakImageUrl,candidate.sourceName+' peak')+'</div><div class="muscles">主练：'+esc(candidate.sourcePrimaryMuscles.join(' / ')||'未标注')+'<br>次要：'+esc(candidate.sourceSecondaryMuscles.join(' / ')||'未标注')+'</div><div class="scores">'+scoreHtml('名称',score.nameScore)+scoreHtml('器械',score.equipmentScore)+scoreHtml('主肌群',score.primaryMuscleScore)+scoreHtml('次肌群',score.secondaryMuscleScore)+scoreHtml('属性',score.attributeScore)+scoreHtml('冲突扣分',score.conflictPenalty)+scoreHtml('总分',score.finalConfidence)+'</div><div class="signals">'+signalHtml('match','一致项',score.keyMatches)+signalHtml('difference','差异项',score.keyDifferences)+signalHtml('conflict','硬冲突',score.conflicts.map(conflict=>conflict.message))+'<p><b>推荐操作：</b>'+esc(item.recommendedAction)+'</p></div></button>'}
    function imageHtml(url,alt){return url?'<div class="image-slot"><img loading="lazy" referrerpolicy="no-referrer" src="'+esc(url)+'" alt="'+esc(alt)+'"></div>':'<div class="image-slot">无图片</div>'}
    function scoreHtml(label,value){return '<span class="score"><span>'+label+'</span><b>'+Number(value).toFixed(3)+'</b></span>'}
    function signalHtml(className,label,values){return values.length?'<p class="'+className+'"><b>'+label+'：</b>'+esc(values.join('；'))+'</p>':'<p class="'+className+'"><b>'+label+'：</b>无</p>'}
    function disabled(value){return value?'disabled':''}
    function reuseForm(item,selected){const existing=state.reuse[item.exercise.exerciseId];return '<div class="reuse-form '+(reuseOpenFor===item.exercise.exerciseId?'open':'')+'" id="reuse-form"><label class="field">baseExerciseId<input id="reuse-base" value="'+esc(existing?.baseExerciseId||'')+'" placeholder="另一个 MuscleMap exerciseId"></label><label class="field">sourceId<input id="reuse-source" value="'+esc(existing?.sourceId||selected?.sourceId||'')+'"></label><label class="field wide">共图原因<input id="reuse-reason" value="'+esc(existing?.reason||'')+'" placeholder="为何可以复用基础图片"></label><label class="field wide">差异说明<textarea id="reuse-differences" placeholder="需通过文字说明的握距、把手或脚距差异">'+esc(existing?.differences||'')+'</textarea></label><div class="button-row wide"><button class="btn reuse" id="reuse-save">保存共图决定</button><button class="btn" id="reuse-cancel">取消</button></div></div>'}
    function bindRecordEvents(item,candidates){document.querySelectorAll('.candidate').forEach(element=>element.addEventListener('click',()=>{selectedByExercise[item.exercise.exerciseId]=element.dataset.sourceId;renderRecord(item)}));document.querySelectorAll('.candidate img').forEach(image=>image.addEventListener('error',()=>{const slot=image.parentElement;slot.textContent='图片加载失败'}));document.getElementById('accept')?.addEventListener('click',acceptCurrent);document.getElementById('reject')?.addEventListener('click',rejectCurrent);document.getElementById('force')?.addEventListener('click',forceCurrent);document.getElementById('reuse-toggle')?.addEventListener('click',()=>{reuseOpenFor=item.exercise.exerciseId;renderRecord(item)});document.getElementById('reuse-cancel')?.addEventListener('click',()=>{reuseOpenFor=null;renderRecord(item)});document.getElementById('reuse-save')?.addEventListener('click',saveReuse);document.getElementById('skip')?.addEventListener('click',skipCurrent);document.getElementById('note')?.addEventListener('input',event=>{const value=event.target.value;if(value)state.notes[item.exercise.exerciseId]=value;else delete state.notes[item.exercise.exerciseId];persist()})}
    function acceptCurrent(){const item=currentItem();const candidate=item&&selectedCandidate(item);if(!candidate)return;const id=item.exercise.exerciseId;setFinal(id,'accepted',candidate.sourceId);state.rejected[id]=(state.rejected[id]||[]).filter(sourceId=>sourceId!==candidate.sourceId);if(!state.rejected[id].length)delete state.rejected[id];persist();showToast('已接受 '+candidate.sourceId);goNextUnreviewed(true)}
    function forceCurrent(){const item=currentItem();const candidate=item&&selectedCandidate(item);if(!candidate)return;const id=item.exercise.exerciseId;setFinal(id,'forced',candidate.sourceId);state.rejected[id]=(state.rejected[id]||[]).filter(sourceId=>sourceId!==candidate.sourceId);if(!state.rejected[id].length)delete state.rejected[id];persist();showToast('已强制采用 '+candidate.sourceId);goNextUnreviewed(true)}
    function rejectCurrent(){const item=currentItem();const candidate=item&&selectedCandidate(item);if(!candidate)return;const id=item.exercise.exerciseId;state.rejected[id]=unique([...(state.rejected[id]||[]),candidate.sourceId]);if(state.accepted[id]===candidate.sourceId)delete state.accepted[id];if(state.forced[id]===candidate.sourceId)delete state.forced[id];if(state.reuse[id]?.sourceId===candidate.sourceId)delete state.reuse[id];const next=candidatesFor(item).find(value=>!isRejected(id,value.sourceId));if(next)selectedByExercise[id]=next.sourceId;persist();showToast('已拒绝 '+candidate.sourceId);if(statusOf(item)==='rejected-all')goNextUnreviewed(true);else render()}
    function saveReuse(){const item=currentItem();const candidate=item&&selectedCandidate(item);if(!item||!candidate)return;const base=document.getElementById('reuse-base').value.trim();const sourceId=document.getElementById('reuse-source').value.trim();if(!exerciseIds.has(base)||base===item.exercise.exerciseId){showToast('baseExerciseId 必须是另一个现有动作');return}if(!sourceId){showToast('sourceId 不能为空');return}setFinal(item.exercise.exerciseId,'reuse',{baseExerciseId:base,sourceId,reason:document.getElementById('reuse-reason').value.trim(),differences:document.getElementById('reuse-differences').value.trim()});reuseOpenFor=null;persist();showToast('已标记共图');goNextUnreviewed(true)}
    function skipCurrent(){const item=currentItem();if(!item)return;state.skipped=unique([...state.skipped,item.exercise.exerciseId]);persist();showToast('已标记稍后处理');move(1)}
    function move(delta){const filtered=filteredMatches();if(!filtered.length)return;const index=Math.max(0,filtered.findIndex(item=>item.exercise.exerciseId===currentId));currentId=filtered[(index+delta+filtered.length)%filtered.length].exercise.exerciseId;reuseOpenFor=null;render();scrollWorkspace()}
    function goNextUnreviewed(afterDecision=false){const filtered=filteredMatches();const found=filtered.findIndex(item=>item.exercise.exerciseId===currentId);const start=found>=0?found:-1;for(let offset=afterDecision?1:0;offset<=filtered.length;offset++){const item=filtered[(start+offset+filtered.length)%filtered.length];if(item&&!isComplete(item)){currentId=item.exercise.exerciseId;reuseOpenFor=null;render();scrollWorkspace();return}}render()}
    function goFirstUnreviewed(){const item=filteredMatches().find(value=>!isComplete(value));if(item){currentId=item.exercise.exerciseId;render();scrollWorkspace()}else showToast('当前筛选范围内没有未审核动作')}
    function scrollWorkspace(){document.querySelector('.workspace').scrollIntoView({block:'start',behavior:'smooth'})}
    function selectCandidateNumber(number){const item=currentItem();const candidate=item&&candidatesFor(item)[number-1];if(candidate&&!isRejected(item.exercise.exerciseId,candidate.sourceId)){selectedByExercise[item.exercise.exerciseId]=candidate.sourceId;renderRecord(item)}}
    function fillSelect(id,values){const select=document.getElementById(id);for(const value of values){const option=document.createElement('option');option.value=value;option.textContent=value;select.append(option)}}
    function setTiers(values){const selected=new Set(values);document.querySelectorAll('input[name="tier"]').forEach(input=>{input.checked=selected.has(input.value)})}
    function resetFilters(){document.getElementById('query').value='';document.getElementById('review-status').value='';document.getElementById('media').value='';document.getElementById('equipment').value='';document.getElementById('muscle').value='';setTiers(Object.keys(tierOrder));conflictsOnly=false;render()}
    function handleQuick(action){if(action==='strong'){setTiers(['exact','high-confidence']);document.getElementById('review-status').value='';conflictsOnly=false}else if(action==='manual'){setTiers(['manual-review']);document.getElementById('review-status').value='';conflictsOnly=false}else if(action==='unreviewed'){document.getElementById('review-status').value='unreviewed';conflictsOnly=false}else if(action==='conflicts'){document.getElementById('review-status').value='';conflictsOnly=true}else if(action==='clear'){resetFilters();return}render()}
    function jumpToId(){const id=document.getElementById('jump-id').value.trim();if(!exerciseIds.has(id)){showToast('未找到 exerciseId：'+id);return}resetFilters();currentId=id;render();scrollWorkspace()}
    function exportOverrides(){downloadJson('manual-overrides.json',exportableOverrides());showToast('已导出 manual-overrides.json')}
    function exportableOverrides(){return {version:1,updatedAt:new Date().toISOString(),accepted:state.accepted,rejected:state.rejected,forced:state.forced,reuse:state.reuse,notes:state.notes}}
    function reviewStats(){return {reportExerciseCount:matches.length,exactAndHighConfidenceCount:matches.filter(item=>item.tier==='exact'||item.tier==='high-confidence').length,accepted:Object.keys(state.accepted).length,forced:Object.keys(state.forced).length,reuse:Object.keys(state.reuse).length,rejectedAll:matches.filter(item=>statusOf(item)==='rejected-all').length,skipped:state.skipped.length,unreviewed:matches.filter(item=>statusOf(item)==='unreviewed').length,estimatedIntegrable:Object.keys(state.accepted).length+Object.keys(state.forced).length+Object.keys(state.reuse).length}}
    function exportReviewSummary(){downloadJson('review-summary.json',{version:1,generatedAt:new Date().toISOString(),statistics:reviewStats(),finalDecisions:exportableOverrides(),skipped:state.skipped});showToast('已导出 review-summary.json')}
    function downloadJson(name,value){const blob=new Blob([JSON.stringify(value,null,2)+'\\n'],{type:'application/json;charset=utf-8'});const url=URL.createObjectURL(blob);const link=document.createElement('a');link.href=url;link.download=name;document.body.append(link);link.click();link.remove();setTimeout(()=>URL.revokeObjectURL(url),0)}
    function validateImportShape(value){if(!value||typeof value!=='object'||Array.isArray(value))throw new Error('根节点必须是对象');for(const field of ['accepted','rejected','forced','reuse','notes'])if(value[field]!==undefined&&(typeof value[field]!=='object'||value[field]===null||Array.isArray(value[field])))throw new Error(field+' 必须是对象');return value}
    function mergeState(base,incoming){const result=sanitizeState({...base,accepted:{...base.accepted,...incoming.accepted},rejected:{...base.rejected},forced:{...base.forced,...incoming.forced},reuse:{...base.reuse,...incoming.reuse},notes:{...base.notes,...incoming.notes},skipped:unique([...base.skipped,...incoming.skipped])});for(const [id,values] of Object.entries(incoming.rejected))result.rejected[id]=unique([...(result.rejected[id]||[]),...values]);for(const [id,value] of Object.entries(incoming.reuse))setFinalOn(result,id,'reuse',value);for(const [id,value] of Object.entries(incoming.accepted))setFinalOn(result,id,'accepted',value);for(const [id,value] of Object.entries(incoming.forced))setFinalOn(result,id,'forced',value);return result}
    function setFinalOn(target,id,type,value){delete target.accepted[id];delete target.forced[id];delete target.reuse[id];target[type][id]=value}
    function applyImport(mode){state=mode==='merge'?mergeState(state,pendingImport):sanitizeState(pendingImport);pendingImport=null;persist();document.getElementById('import-dialog').close();showToast(mode==='merge'?'导入内容已合并':'审核状态已完全覆盖');render()}
    function showToast(message){const toast=document.getElementById('toast');if(!toast)return;toast.textContent=message;toast.classList.add('show');clearTimeout(showToast.timer);showToast.timer=setTimeout(()=>toast.classList.remove('show'),2200)}

    fillSelect('media',['complete','partial','missing']);fillSelect('equipment',unique(matches.flatMap(item=>item.exercise.equipment)));fillSelect('muscle',unique(matches.flatMap(item=>item.exercise.primaryMuscles)));
    document.querySelectorAll('.toolbar input,.toolbar select').forEach(control=>control.addEventListener('input',render));document.querySelectorAll('[data-quick]').forEach(button=>button.addEventListener('click',()=>handleQuick(button.dataset.quick)));
    document.getElementById('previous').addEventListener('click',()=>move(-1));document.getElementById('next').addEventListener('click',()=>move(1));document.getElementById('next-unreviewed').addEventListener('click',()=>goNextUnreviewed(true));document.getElementById('first-unreviewed').addEventListener('click',goFirstUnreviewed);document.getElementById('jump-button').addEventListener('click',jumpToId);document.getElementById('jump-id').addEventListener('keydown',event=>{if(event.key==='Enter')jumpToId()});
    document.getElementById('export-overrides').addEventListener('click',exportOverrides);document.getElementById('export-summary').addEventListener('click',exportReviewSummary);document.getElementById('import-button').addEventListener('click',()=>document.getElementById('import-file').click());document.getElementById('import-file').addEventListener('change',async event=>{const file=event.target.files?.[0];if(!file)return;try{pendingImport=sanitizeState(validateImportShape(JSON.parse(await file.text())));document.getElementById('import-dialog').showModal()}catch(error){showToast('导入失败：'+(error instanceof Error?error.message:'JSON 格式无效'))}event.target.value=''});document.getElementById('import-merge').addEventListener('click',()=>applyImport('merge'));document.getElementById('import-replace').addEventListener('click',()=>applyImport('replace'));document.getElementById('import-cancel').addEventListener('click',()=>{pendingImport=null;document.getElementById('import-dialog').close()});document.getElementById('clear-progress').addEventListener('click',()=>{if(confirm('确定清空当前报告的全部本地审核进度吗？此操作无法撤销。')){localStorage.removeItem(STORAGE_KEY);state=sanitizeState(payload.initialOverrides);currentId=null;selectedByExercise={};render();showToast('当前审核进度已清空')}});
    document.addEventListener('keydown',event=>{if(event.target.matches('input,textarea,select,button')||event.target.isContentEditable)return;const key=event.key.toLowerCase();if(key==='a')acceptCurrent();else if(key==='r')rejectCurrent();else if(key==='f')forceCurrent();else if(key==='g'){const item=currentItem();if(item){reuseOpenFor=item.exercise.exerciseId;renderRecord(item)}}else if(key==='s')skipCurrent();else if(key==='arrowleft'||key==='k')move(-1);else if(key==='arrowright'||key==='j')move(1);else if(['1','2','3'].includes(key))selectCandidateNumber(Number(key));else if(key==='e')exportOverrides()});
    render();
  </script>
</body></html>`;
}

function emptyOverrides(): ManualOverrides {
  return { version: 1, updatedAt: null, accepted: {}, rejected: {}, forced: {}, reuse: {}, notes: {} };
}

function escapeHtml(value: string) {
  return value.replace(/[&<>"']/g, (character) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#039;' })[character] ?? character);
}
